name: ci
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install -r requirements.txt
      - run: pytest -q
# cache pip and the Playwright browser
# run pytest and ruff on every push
# upload coverage on pull requests
# minor workflow tweak
# minor workflow tweak
# minor workflow tweak
# minor workflow tweak
# upload coverage on pull requests
# cache pip and the Playwright browser
# minor workflow tweak
